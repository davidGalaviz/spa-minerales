Para levantar el API, ejecutar el siguiente comando desde la carpeta "api".

```
node index.js
```

Para servir el front-end, ejecutar el siguiente comando desde la carpeta "frontend".

```
npx live-server
```
